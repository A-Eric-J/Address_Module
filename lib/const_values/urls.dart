import 'package:address_module/const_values/config.dart';

/// URLs refers to all the endpoints that are using in this app
class URLs {
  static const mapSearch = '${mapBaseUrl}search/v2/autocomplete';
  static const mapAddress = '${mapBaseUrl}reverse/no/?';
}
